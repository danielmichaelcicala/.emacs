preamble_\documentclass[11pt]{amsart}

% load packages ----------------
\usepackage{amsfonts, amsthm, amssymb, amsmath, stmaryrd, etoolbox}
\usepackage{mathtools}
\usepackage{graphicx,caption,subcaption}
\usepackage{xcolor}
\usepackage{multicol}
\usepackage[margin=0.75in]{geometry}
\usepackage[inline]{enumitem}
\setlist{itemsep=0em, topsep=0em, parsep=0em}
\setlist[enumerate]{label=(\alph*)}
\usepackage{tikz}
\usetikzlibrary{matrix,arrows,shapes}

% environments ---------------------
\theoremstyle{definition}
\newtheorem{q}{} 

% begin document -------------------
\begin{document}
\thispagestyle{empty}

% left header material -------------
\begin{flushleft}
  Homework 02---Inverse Functions
\end{flushleft}

\begin{q}
  Find the largest intervals on which $$f(x)=(x-2)^2$$ has an inverse. (hint: graph the function using a graphing utility)
  \begin{enumerate}
  \item $$(-\infty,2)\cup(2,\infty)$$	
  \item $$(-\infty,\infty)$$	
  \item $$(2,\infty)$$	
  \item $$(0,2)$$
  \end{enumerate}
\end{q}

MC		
MC	Find the inverse of the function $$f(x)=5x+5$$.	$$f^{-1}(x)=\frac{x}{5}-1$$	correct	$$f^{-1}(x)=\frac{x}{5}-5$$	incorrect	$$f^{-1}(x)=5x-5$$	incorrect
MC	Find the inverse of the function $$f(x)=\frac{2}{x^2+1}$$ defined on $$x \geq 0$$.	$$f^{-1}(x)=\sqrt{\frac{2}{x}-1}$$	correct	$$f^{-1}(x)=\sqrt{\frac{2}{x}-x}$$	incorrect	$$f^{-1}(x)=\frac{2}{x^2-1}$$	incorrect
MC	Find the inverse of the function $$f(x)=(x-2)^2-1$$ defined on $$x \geq 2$$.	$$f^{-1}(x)=\sqrt{x+1}+2$$	correct	$$f^{-1}(x)=\sqrt{x+1}$$	incorrect	$$f^{-1}(x)=(x+2)^2+1$$	incorrect
MC	Find the inverse of the function $$f(x)=\ln (3x+4)$$	$$f^{-1}(x)=\frac{e^x-4}{3}$$	correct	$$f^{-1}(x)=3x-4$$	incorrect	$$f^{-1}(x)=\frac{e^x}{3}-4$$	incorrect
MC	Find the inverse of the function $$f(x)=2^{3x}+2$$	$$f^{-1}(x)=\log_2 ((x-2)^{1/3})$$	correct	$$f^{-1}(x)=\ln (3x-2)$$	incorrect	$$f^{-1}(x)=\frac{\log_2(x)-2}{3}$$	incorrect
MC	Assume that $$\log_b(x)=2$$, $$\log_b(y)=3$$ and $$\log_b(z)=4$$. Compute $$\log_b (\frac{x}{y})$$.	$$-1$$	correct	$$2/3$$	incorrect	$$\log_b(2/3)$$	incorrect
MC	Assume that $$\log_b(x)=2$$, $$\log_b(y)=3$$ and $$\log_b(z)=4$$. Compute $$\log_b (xz)$$.	$$6$$	correct	$$8$$	incorrect	$$\log_b(8)$$	incorrect
MC	Assume that $$\log_b(x)=2$$, $$\log_b(y)=3$$ and $$\log_b(z)=4$$. Compute $$\log_b \left(\left( \frac{xy}{z} \right)^2 \right)$$.	$$2$$	correct	$$9/4$$	incorrect	$$\log_b(9/4)$$	incorrect
MC	Assume that $$\log_b(x)=2$$, $$\log_b(y)=3$$ and $$\log_b(z)=4$$. Compute $$\log_b (\sqrt{bx})$$.	$$-1$$	correct	not enough information	incorrect	$$\log_b(\sqrt{2})$$	incorrect
FIB	What is the value of $$\log_2 (2^3)$$? (Hint: Use that $$f(x)=\log_2(x)$$ and $$g(x)=2^x$$ are inverses)	3
FIB	What is the value of $$\ln (e^2)$$? (Hint: Use that $$f(x)=\ln (x)$$ and $$g(x)=e^x$$ are inverses)	2
FIB	What is the value of $$e^{\ln (a)}$$? (Hint: Use that $$f(x)=\ln(x)$$ and $$g(x)=e^x$$ are inverses)	a
FIB	What is the value of $$5^{\log_5 (b)}$$ (Hint: Use that $$f(x)=\log_5(x)$$ and $$g(x)=5^x$$ are inverses)	b


% end document------------------
\end{document}
